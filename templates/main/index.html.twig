{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Accueil{% endblock %}

{% block body %}
    <h2>Liste des sorties</h2>
    <div class="filterForm">
        {{ form_start(filterForm) }}
        {{ form_widget(filterForm) }}

        {{ form_end(filterForm) }}
    </div>
    <table class="table table-sm mainTable">
    <thead>
    <tr>
        <th scope="col">Nom de la sortie</th>
        <th scope="col" >Date sortie</th>
        <th scope="col" >Date de cloture</th>
        <th scope="col" class="d-none d-sm-table-cell">Inscrits/Places</th>
        <th scope="col" class="d-none d-sm-table-cell">Etat</th>
        <th scope="col" class="d-none d-lg-table-cell">Organisateur</th>
        <th scope="col" class="d-none d-sm-table-cell">Actions</th>
    </tr>
    </thead>
    <tbody>
        {% for sortie in sorties %}
                <tr class="sortieLine">
                    <td><a href="{{ path('sortie_details', {'id': sortie.id}) }}">{{ sortie.nom }}</a></td>
                    <td>{{ sortie.dateHeureDebut|date('d M Y', 'Europe/Paris') }}</td>
                    <td>{{ sortie.dateLimiteInscription|date('d M Y', 'Europe/Paris') }}</td>
                    <td class="d-none d-sm-table-cell"><span class="participants-length "  onclick="sortParticipants();">{{ sortie.participants|length }}</span>/{{ sortie.nbInscriptionMax }}</td>
                    <td class="d-none d-sm-table-cell">{{ sortie.etat.libelle }}</td>
                    <td class="d-none d-lg-table-cell"><a href="{{ path('profil_home', {'id': sortie.organisateur.id}) }}">{{ sortie.organisateur.pseudo }}</a></td>
                    <td class="d-none d-sm-table-cell">
                        {% if sortie.dateHeureDebut > date('now', '- 30 days') %}
                            <a href="{{ path('sortie_details', {'id': sortie.id}) }}">Afficher</a>
                        {% endif %}

                        {% if app.user  and sortie.etat.libelle != 'Annuler'%}
                            {% if imIn[sortie.id] %}
                                {% if sortie.dateHeureDebut > date('now') %}
                                    <a href="{{ path('main_desister', {'id': sortie.id}) }}">Se desister</a>
                                {% endif %}
                            {% else %}
                                {% if sortie.dateLimiteInscription > date('now') and sortie.nbInscriptionMax > sortie.participants|length %}
                                    <a href="{{ path('main_participe', {'id': sortie.id}) }}">S'inscrire</a>
                                {% endif %}
                            {% endif %}
                        {% endif %}
                    </td>
                </tr>
        {% else %}
            <p>Aucune sortie de prévu !</p>
        {% endfor %}
    </tbody>
    </table>
    {% if app.user %}
        <a href="{{ path('sortie_create') }}"><button class="btn size_btn">Ajouter une sortie</button></a>
    {% endif %}
    <div class="navigation">
        {{ knp_pagination_render(sorties) }}
    </div>

{% endblock %}
